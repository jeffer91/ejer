{
  "name": "fitness-jeff",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Fitness Jeff: app personal de progreso, entrenamiento, hidratación y recomendaciones.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "audit": "npm audit --audit-level=high",
    "cloudflare:check": "wrangler pages functions build functions --outfile .wrangler/pages-functions.js",
    "check": "npm run typecheck && npm run test && npm run build",
    "icons:generate": "tauri icon public/icon.svg",
    "tauri": "tauri",
    "tauri:dev": "npm run icons:generate && tauri dev",
    "tauri:build": "npm run icons:generate && tauri build",
    "android:init": "npm run icons:generate && tauri android init",
    "android:dev": "npm run icons:generate && tauri android dev",
    "android:build": "npm run icons:generate && tauri android build",
    "cloudflare:dev": "wrangler pages dev dist",
    "cloudflare:deploy": "npm run build && wrangler pages deploy dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.110.8",
    "@tauri-apps/api": "2.11.1",
    "@tauri-apps/plugin-deep-link": "2.4.9",
    "@tauri-apps/plugin-opener": "2.5.4",
    "react": "19.2.7",
    "react-dom": "19.2.7"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4",
    "@types/react": "19.2.17",
    "@types/react-dom": "19.2.3",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10",
    "wrangler": "4.120.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
